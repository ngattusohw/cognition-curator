{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "flask db upgrade && gunicorn --bind 0.0.0.0:8080 --workers 2 --worker-class sync --timeout 120 --access-logfile - --error-logfile - --log-level info src.app:app",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
